---
import Page from './Page.astro';
const { frontmatter } = Astro.props;
---
<Page pageTitle={frontmatter.name}>
  <img src={frontmatter.image.url} alt={frontmatter.image.alt} style="display: block; margin:0 auto 1em"/>
  <slot />
</Page>
